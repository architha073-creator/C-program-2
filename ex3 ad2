#include <stdio.h>
 #include <conio.h>
 int main()
 {
	int i,sum=0,n,num;
	clrscr();
	printf("Enter a no:");
	scanf("%d",&n);
	num=n;
	while(i!=0)
	{
		 i=n%10;
		 sum=sum+(i*i*i);
		 n=n/10;
	}
	if (sum==num)
	{
		printf("Armstrong:");
	}
	else
	{
		printf("not a armstrong:");
	}
	getch();
	
